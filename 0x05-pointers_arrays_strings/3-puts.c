#include "main.h"

/**
 * _puts - prints a string followed by a new line
 * @str: input string
 * Return: Output a string
 */
void _puts(char *str)
{
	while (*str)
	{
		_putchar(*s);
		str++;
	}
	_putchar('\n');
}
