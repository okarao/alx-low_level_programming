#include "main.h"
#include <ctype.h>
/**
 * mul -  multiply two numbers
 * @a: input character
 * @b: input character
 * Return: return the product of two integers
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);
	return (c);
}
