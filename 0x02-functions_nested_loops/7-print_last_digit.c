#include "main.h"
#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
/**
 * print_last_digit - Entry point
 * @n: intput character
 * Return: Always 0 (success)
 */
int print_last_digit(int n)
{
	return (n % 10);
}
