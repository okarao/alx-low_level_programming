#include "main.h"
#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
/**
 * add - Entry point
 * @n: input variable
 * @m: input add two integers and return the result
 * Return: Always 0 (success)
 */
int add(int n, int m)
{
	int sum;

	sum = (n + m);
	return (sum);
}
