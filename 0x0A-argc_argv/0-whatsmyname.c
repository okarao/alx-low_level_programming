#include "main.h"

/**
 * main -  entry point
 * @argc: argument count
 * @argv: argument vector
 * Return: Prints the name of the program
 */
int main(int argc, char *argv[])
{
	argc = 0;
	
	printf("%s\n", argv[argc]);
	return (0);
}
